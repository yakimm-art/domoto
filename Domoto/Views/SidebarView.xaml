<UserControl x:Class="Domoto.Views.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="260"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Border Background="{DynamicResource AppSidebarBg}"
            BorderBrush="{DynamicResource AppSidebarBorder}"
            BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <!-- App Title with decorative element -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,12">
            <TextBlock Text="Domoto"
                       FontFamily="{StaticResource NeoFontFamily}"
                       FontSize="{StaticResource NeoFontHeading}"
                       FontWeight="Bold"
                       Foreground="{StaticResource NeoTextLight}"/>
            <TextBlock Text=" ✦"
                       FontSize="14"
                       Foreground="{StaticResource NeoAccentTeal}"
                       VerticalAlignment="Top"
                       Margin="4,4,0,0"/>
        </StackPanel>

        <!-- Search Bar -->
        <TextBox Grid.Row="1"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 FontFamily="{StaticResource NeoFontFamily}"
                 FontSize="{StaticResource NeoFontBodySmall}"
                 Background="#2A2A3D"
                 Foreground="{StaticResource NeoTextLight}"
                 BorderBrush="#3A3A50"
                 BorderThickness="2"
                 Padding="10,8"
                 Margin="16,0,16,16"
                 ToolTip="Search tasks..."
                 AutomationProperties.Name="Search tasks"/>

            <!-- NAV -->
            <StackPanel Grid.Row="2" Margin="0,4,0,0">
                <TextBlock Text="MENU"
                           FontFamily="{DynamicResource AppFont}"
                           FontSize="10" FontWeight="Bold"
                           Foreground="{DynamicResource AppSidebarMuted}"
                           Margin="22,4,20,8"/>

                <!-- Section Header -->
                <TextBlock Text="NAVIGATION"
                           FontFamily="{StaticResource NeoFontFamily}"
                           FontSize="{StaticResource NeoFontLabelSmall}"
                           FontWeight="Bold"
                           Foreground="#6C6C8A"
                           Margin="20,0,20,8"/>

                <!-- Separator line between header and nav items -->
                <Border Height="1" Background="#3A3A50" Margin="16,0,16,8"/>

                <!-- Home -->
                <Button Command="{Binding NavigateHomeCommand}" Cursor="Hand" Margin="0,0,0,2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="NavBorder"
                                                Background="Transparent"
                                                BorderThickness="3,0,0,0"
                                                BorderBrush="Transparent"
                                                Padding="20,10,16,10">
                                            <TextBlock Text="🏠  Home"
                                                       FontFamily="{StaticResource NeoFontFamily}"
                                                       FontSize="{StaticResource NeoFontBody}"
                                                       Foreground="{StaticResource NeoTextLight}"/>
                                        </Border>
                                        <TextBlock Text="Dashboard"
                                                   Foreground="{DynamicResource AppSidebarText}"
                                                   FontFamily="{DynamicResource AppFont}"
                                                   FontSize="13" FontWeight="Medium"
                                                   VerticalAlignment="Center"/>
                                    </DockPanel>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background"
                                            Value="{DynamicResource AppSidebarHover}"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding ActiveNavItem}" Value="Home">
                                    <Setter TargetName="bd" Property="Background"
                                            Value="{DynamicResource AppSidebarActive}"/>
                                    <Setter TargetName="rail" Property="Background"
                                            Value="{DynamicResource AppAccent}"/>
                                    <Setter TargetName="iconBg" Property="Background"
                                            Value="{DynamicResource AppAccent}"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Tasks with count badge -->
                <Button Command="{Binding NavigateTasksCommand}" Cursor="Hand" Margin="0,0,0,2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="NavBorder"
                                                Background="Transparent"
                                                BorderThickness="3,0,0,0"
                                                BorderBrush="Transparent"
                                                Padding="20,10,16,10">
                                            <DockPanel>
                                                <!-- Task count badge on the right -->
                                                <Border DockPanel.Dock="Right"
                                                        Background="{StaticResource NeoAccentBlue}"
                                                        CornerRadius="9"
                                                        Padding="7,2"
                                                        VerticalAlignment="Center"
                                                        Margin="8,0,4,0">
                                                    <TextBlock Text="6"
                                                               FontFamily="{StaticResource NeoFontFamily}"
                                                               FontSize="10" FontWeight="Bold"
                                                               Foreground="White"
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                                <TextBlock Text="📋  Tasks"
                                                           FontFamily="{StaticResource NeoFontFamily}"
                                                           FontSize="{StaticResource NeoFontBody}"
                                                           Foreground="{StaticResource NeoTextLight}"/>
                                            </DockPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding ActiveNavItem}" Value="Tasks">
                                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource NeoAccentBlue}"/>
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Profile -->
                <Button Command="{Binding NavigateProfileCommand}" Cursor="Hand" Margin="0,0,0,2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="NavBorder"
                                                Background="Transparent"
                                                BorderThickness="3,0,0,0"
                                                BorderBrush="Transparent"
                                                Padding="20,10,16,10">
                                            <TextBlock Text="👤  Profile"
                                                       FontFamily="{StaticResource NeoFontFamily}"
                                                       FontSize="{StaticResource NeoFontBody}"
                                                       Foreground="{StaticResource NeoTextLight}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding ActiveNavItem}" Value="Profile">
                                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource NeoAccentBlue}"/>
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Admin (visible only when IsAdmin is true) -->
                <Button Command="{Binding NavigateAdminCommand}" Cursor="Hand" Margin="0,0,0,2"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="NavBorder"
                                                Background="Transparent"
                                                BorderThickness="3,0,0,0"
                                                BorderBrush="Transparent"
                                                Padding="20,10,16,10">
                                            <TextBlock Text="🔧  Admin"
                                                       FontFamily="{StaticResource NeoFontFamily}"
                                                       FontSize="{StaticResource NeoFontBody}"
                                                       Foreground="{StaticResource NeoTextLight}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding ActiveNavItem}" Value="Admin">
                                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource NeoAccentBlue}"/>
                                                <Setter TargetName="NavBorder" Property="Background" Value="#2A2A3D"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Upgrade promo card (light polish, optional) -->
                <Border Margin="16,24,16,0"
                        Background="{DynamicResource AppAccentSubtle}"
                        CornerRadius="12"
                        Padding="14">
                    <StackPanel>
                        <TextBlock Text="✦ Pro tip"
                                   FontFamily="{DynamicResource AppFont}"
                                   FontSize="10" FontWeight="Bold"
                                   Foreground="{DynamicResource AppAccent}"
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="Use priorities and due dates to sort your day."
                                   FontFamily="{DynamicResource AppFont}"
                                   FontSize="11"
                                   Foreground="{DynamicResource AppText}"
                                   TextWrapping="Wrap"
                                   LineHeight="16"/>
                    </StackPanel>
                </Border>
            </StackPanel>

        <!-- User Info + Logout at bottom -->
        <Border Grid.Row="3"
                BorderBrush="#3A3A50"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Command="{Binding LogoutCommand}"
                        Content="Logout"
                        FontFamily="{StaticResource NeoFontFamily}"
                        FontSize="{StaticResource NeoFontLabelSmall}"
                        FontWeight="Bold"
                        Foreground="{StaticResource NeoAccentPink}"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="Logout">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="LogoutBorder" Background="Transparent"
                                                CornerRadius="6" Padding="8,4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="LogoutBorder" Property="Background" Value="#3D2A2A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- User avatar circle -->
                    <Border Width="32" Height="32" CornerRadius="16"
                            Background="{StaticResource NeoAccentBlue}"
                            Margin="0,0,10,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUsername, Converter={StaticResource FirstLetterConverter}}"
                                   FontFamily="{StaticResource NeoFontFamily}"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUsername}"
                                   FontFamily="{StaticResource NeoFontFamily}"
                                   FontSize="{StaticResource NeoFontBody}"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource NeoTextLight}"/>
                        <TextBlock Text="{Binding CurrentRole}"
                                   FontFamily="{StaticResource NeoFontFamily}"
                                   FontSize="{StaticResource NeoFontLabelSmall}"
                                   Foreground="#6C6C8A"/>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
